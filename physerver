import socket

newsocket = socket.socket()
newhost = socket.gethostname()
newport = 1029

newsocket.bind((newhost, newport))

newsocket.(listen(10))

while true:
  conn, addr = sock.accept()
  print('Got a connection from', addr)
  conn.send(b' you have been connected to our server')
  conn.close()








